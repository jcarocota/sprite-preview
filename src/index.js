import { App } from "./app"

const app = new App();
app.run();


//const mainScene = PIXI.Container();
//this.app.stage.addChild(mainScene);

/*app.loader
    .add('character', 'assets/spineboy.json')
    .load(function (loader, resources) {
        //const animation = new Spine(resources.character.spineData);
        //mainScene.addChild(animation);

        //animation.x = 400;
        //animation.y = 400;

        //animation.state.setAnimation(0, "walk",true)

        
    });*/
